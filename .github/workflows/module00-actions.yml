name: Module 01 Tests

on:
  push:

  pull_request:

jobs:
  build-and-test:
    runs-on: self-hosted

    strategy:
      matrix:
        folder:
          - 'Modules/Module00/ex02'
          - 'Modules/Module00/ex03'
    steps:
    - uses: actions/checkout@v3

    - name: Compile executable
      run: |
        echo ${{ matrix.folder }}
        cd ${{ matrix.folder }}
        make

    - name: Run tests
      run: |
        make test
